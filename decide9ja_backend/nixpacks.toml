# Nixpacks configuration for Railway deployment
# This ensures Tesseract OCR is installed on the container

[phases.setup]
nixPkgs = ["tesseract", "tesseract-data-eng"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
